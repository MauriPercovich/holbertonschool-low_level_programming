#include "main.h"
/**
 * _puts_recursion - prints a string, followed by a new line.
 * @s: string
 * Return: always 0
**/
void _puts_recursion(char *s)
{
int x;

_putchar(s[x]);
x++;
if (s[x] != '\0') 
{
_putchar(s[x]);
}


int main(void)
{
_putchar(s[x]);
return (0);

}
