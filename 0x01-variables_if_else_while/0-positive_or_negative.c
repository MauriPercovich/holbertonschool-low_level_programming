#include <stdlib.h>
#include <time.h>
/**
 * main - printing
 *
 * Return: 0
 **/
int main(void)
{
		int n;

			srand(time(0));
				n = rand() - RAND_MAX / 2;
					if(n > 0){
					int puts
					("is positive")}
					else {
					int puts("is negative")}
					if(n = 0) {
					int puts("is zero")		
					return (0);
}
